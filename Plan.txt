I need to create cycle conserving Earliest Deadline First scheduler in C.Select_Frequency - the core of the Entire thing.We select a new frequency every time a new task is added or release
Important terms - Ti - task, Pi - associated period, Ci - Worst case computation time, basically assume all tasks take the worst case computation time and are released every Pi time units (Ci is in Cycles) - Ci/Pi is utilisation of the given project 
fm - fmax. ratio of fm is the maximum amount of cycles that can be provided. f/fm is the fraction of cycles that can be used by computer, and they should be greater than or equal to the net utilisations of all periodic tasks available.We also dynamically update the time taken for a specific task so that, we can use a reduced clock frequency UNTIL The Task is released again , which means we go back to worst case Utilisation.IDEALLY, every single task will have its worst case utilisations used to calcuate the frequency required.Once a task is finished, the Utilisation set is updated with the actual utilisation and frequecy is selected again , which means that the next selection can have a lower frequency.We recalculate to measure the contribution that the task has already had during completion. I.E if a task that was supposed to take 6 cycles, takes 4, the next few tasks should recalculate frequency, but that calculation should also take in to consideration the already elapsed cycles.We do that by calculating how much the task has actually utilised of the time period and use it for segmenting the rest of the tasks. The priority based resource allocation is still the same

